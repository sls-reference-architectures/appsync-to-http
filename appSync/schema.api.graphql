type Query {
  getProduct(input: GetProductInput!): Product!
}

input GetProductInput {
  storeId: String!
  productId: String!
}

type Product {
  name: String!
  price: Int!
  productId: String!
  storeId: String!
}